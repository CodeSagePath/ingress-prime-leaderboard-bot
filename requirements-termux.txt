# We have most packages installed, just skip the problematic ones
pip install python-telegram-bot==21.4
pip install SQLAlchemy==2.0.40
pip install aiosqlite==0.19.0
pip install redis==5.0.4
pip install rq==1.16.1
pip install python-dotenv==1.0.1
pip install APScheduler==3.10.4
# Skip these two - they're for web server functionality you don't need
# pip install fastapi==0.111.0
# pip install uvicorn==0.30.1

